directory:				code/ex09
files to hand in:		ex09.c
allowed functions:		-

Create a function that takes a string and swaps its
halves. If the string has an odd amount of characters the
first half is one bigger. Return the Pointer to the string after
you changed it. Do not return a copy of it.

char	*str_swap_halves(char *str);

Example:
HelloWorld → WorldHello
